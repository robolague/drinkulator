apiVersion: v1
kind: ConfigMap
metadata:
  name: drink-calculator-config
data:
  FLASK_APP: main
  FLASK_ENV: production
  PORT: "5000"
---
apiVersion: v1
kind: Pod
metadata:
  name: drink-calculator
  labels:
    app: drink-calculator
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: /metrics
    prometheus.io/port: "5000"
spec:
  containers:
    - name: drink-calculator
      image: ghcr.io/your-org/drink-calculator:latest
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 5000
          name: http
      envFrom:
        - configMapRef:
            name: drink-calculator-config
      readinessProbe:
        httpGet:
          path: /
          port: http
        initialDelaySeconds: 3
        periodSeconds: 10
      livenessProbe:
        httpGet:
          path: /
          port: http
        initialDelaySeconds: 10
        periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: drink-calculator
  labels:
    app: drink-calculator
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: /metrics
    prometheus.io/port: "5000"
spec:
  selector:
    app: drink-calculator
  ports:
    - name: http
      port: 5000
      targetPort: http
